# Mobile

The app is a PWA and Capacitor Android app.

Commands:

```bash
npm run build:android
npx cap open android
```

Use Android Studio to create signed APK/AAB.

For PWA Builder, deploy the web app first, then generate a package from the public HTTPS URL.
